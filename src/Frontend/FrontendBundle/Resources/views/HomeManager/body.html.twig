{% extends '::layout.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}
{% block javascriptsTop %}
    {{ parent() }}
{% endblock %}
{% block headernav %}
    {{ parent() }}
{% endblock %}
{% block body %}

<div class="cover-home"><h1 class="text-uppercase">coursier à la demande</h1><p>Commandez votre coursier et demandez la livraison de vos médicaments à votre position GPS </p>
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        <button type="button" class="btnmdg  center-block " onclick='window.location="{{ path('add_command') }}"'>COMMANDER UN COURSIER</button>
    {% else %}
        <button type="button" class="btnmdg  center-block " data-toggle="modal" data-target="#login-modal">COMMANDER UN COURSIER</button>
    {% endif %}
</div>
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") == false %}
    <div class="modal fade" id="login-modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display: none;">
        <div class="modal-dialog">
            <div class="loginmodal-container">
                <h1>Connectez-vous à votre compte</h1><br>
                <form id="login-form" action="{{ path("fos_user_security_check") }}" method="post">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                    <input type="text" id="username" name="_username" value="" required="required" tabindex="1" class="form-control" placeholder="{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}" />

                    <input type="password" name="_password" id="password" tabindex="2" class="form-control" placeholder="{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}" required="required" />
                    <input type="checkbox" tabindex="3" id="remember_me" name="_remember_me" value="on" />

                    <label for="remember_me">{{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}</label>
                    <input type="submit" tabindex="4" id="_submit" name="_submit" class="login loginmodal-submit" value="{{ 'security.login.submit'|trans({}, 'FOSUserBundle') }}" />

                </form>

                <div class="login-help">
                    <a href="{{ path('user_Add_user') }}">Vous n'avez pas encore de compte ??</a>
                </div>
            </div>
        </div>
    </div>
    {% endif %}
<div class="container interligne">
    <div class="container">
        <div class="row">
            <div class="col-lg-12"><h3 class="text-center">COMMENT ÇA MARCHE?</h3><br /><br /></div>

            <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12 text-center">

                {% image '@FrontendFrontendBundle/Resources/public/css/pic/cmd.svg' %}
                <img class="center-block" src="{{ asset_url }}" alt="..." />
                {% endimage %}
                <br />

                <p>Commandez un coursier directement sur notre site et demandez vos médicaments.</p>
            </div>

            <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12 text-center">

                {% image '@FrontendFrontendBundle/Resources/public/css/pic/clt.svg' %}
                <img class="center-block" src="{{ asset_url }}" alt="..." />
                {% endimage %}
                <br />

                <p>Un coursier se dirige <b>immédiatement</b> vers vous pour récupérer votre ordonnance si nécessaire sinon <b>directement</b> à la pharmacie.</p>
            </div>

            <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12 text-center ">
                {% image '@FrontendFrontendBundle/Resources/public/css/pic/suiver.svg' %}
                <img class="center-block" src="{{ asset_url }}" alt="..." />
                {% endimage %}
                <br />

                <p>Ne perdez pas votre coursier de vue et suivez l'évolution de votre commande <b>en ligne et en temps réel.</b></p>
            </div>

            <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12 text-center">
                {% image '@FrontendFrontendBundle/Resources/public/css/pic/livrer.svg' %}
                <img class="center-block" src="{{ asset_url }}" alt="..." />
                {% endimage %}
                <br />

                <p>Recevez votre livraison et presentez votre <b>code d'accusé de réception </b> au coursier avec le paiement.</p>
            </div>


        </div>
    </div>
</div>
{% endblock %}
{% block footer %}
    {{ parent() }}
{% endblock %}
